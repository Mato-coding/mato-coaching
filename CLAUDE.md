# CLAUDE.md

Briefing für Claude Code. Lies zu Sitzungsbeginn diese Datei und design-system.md. Halte sie und AGENTS.md widerspruchsfrei. Bei Aufgaben zu Copy, Positionierung, Angebot oder Personendarstellung zusätzlich profil-lasse.md lesen. Bei Aufgaben rund um Erstgespräch, /termin oder /coaching zusätzlich erstgespraech-leitfaden.md lesen. Bei Aufgaben zum digitalen Workbook zusätzlich workbook-konzept.md lesen.

> Stand: 16.08.2026 (Architektur-Refactoring Auftrag 1 von 5 auf Branch refactor/primitives umgesetzt, nach architektur-audit.md: UI-Primitives Section/Container/Heading/Button/Card in src/components/ui/ gebaut, Eyebrow darauf erweitert, Startseite vollständig darauf migriert. Doppel-FadeIn auf der Startseite aufgelöst (Audit-Befund A1), Eyebrow/H2 in LeadMagnet und LeadMagnetForm-Erfolgszustand auf die Standard-Primitives umgestellt (Audit 2.2), font-display-Alias aus globals.css entfernt (ungenutzt), text-[--color-navy] in Method.tsx auf text-accent korrigiert (Audit 2.3 Befund C). Type-Scale- und Container-Tokens in globals.css verdrahtet, siehe Abschnitt "Architektur-Konventionen". Auf main gemergt und live. Architektur-Refactoring Auftrag 2 von 5 auf Branch refactor/breathwork umgesetzt: Heading um Variante emphasis erweitert (Mobil wie section, ab md 48px), Cause.tsx darauf umgestellt, damit entfällt die bisherige Cause-Ausnahme aus den Architektur-Konventionen. Alle Sektionen in src/components/sections/breathwork/ auf Section/Container/Heading/Button umgestellt, Audio-Sektion aus breathwork/page.tsx nach BreathworkAudio.tsx extrahiert (Anchor-ID audio-reset unverändert), tote BreathworkAbout.tsx gelöscht (Audit 2.1 Befund A3). Impressum-E-Mail auf hello@lassekluever.de korrigiert (datenschutz/page.tsx hat weiterhin die alte gmail-Adresse, Korrektur zurückgestellt auf Auftrag 4). Nachtrag zu Auftrag 2: neues Primitive ListMark.tsx in src/components/ui/ (Umber-Hairline-Strich vor Listen-/Schrittpunkten, zentriert per h-[1lh] plus items-center statt geratener margin-top-Werte), ersetzt die handgebauten Marker in BreathworkProcess.tsx, der bausteine-Liste in coaching/page.tsx, Transformation.tsx und der Credentials-Liste in About.tsx. Auf main gemergt und live. Architektur-Refactoring Auftrag 3 von 5 auf Branch refactor/coaching umgesetzt: alle 8 Sektionen von /coaching aus der bisherigen Inline-page.tsx nach src/components/sections/coaching/*.tsx extrahiert und auf Section/Container/Heading/Button/Eyebrow/ListMark umgestellt, page.tsx ist jetzt eine reine Kompositionsliste plus Metadata und unverändertem Inline-JSON-LD. Die Über-mich-Sektion weicht inhaltlich von About.tsx ab (eigener, programmspezifischer Text) und wurde deshalb als eigene CoachingAbout.tsx extrahiert statt als Alias. Gemeinsames Schrittlisten-Markup aus BreathworkProcess.tsx und der bausteine-Liste in StepList.tsx (src/components/ui/) zusammengeführt. Coaching-Hero-H1 und BreathworkHero-H1 waren exakt deckungsgleich (text-5xl md:text-6xl, keine lg-Stufe): dafür neue Heading-Variante display-sub eingeführt (dieselben Tokens wie display, ohne lg-Sprung), die bisherige, nur in BreathworkHero.tsx als Code-Kommentar dokumentierte Ausnahme entfällt damit. Neues src/lib/anchors.ts mit AUDIO_ANCHOR/AUDIO_RESET_ANCHOR/PROGRAMM_ANCHOR, ersetzt alle bisherigen wörtlichen Anchor-Strings (LeadMagnet.tsx, BreathworkAudio.tsx, BreathworkHero.tsx, /coaching). Nachtrag zu Auftrag 3 (Regression gefunden per Rendered-HTML-Diff gegen main): CoachingAbout.tsx hatte versehentlich Container width="default" (1140px) statt der historischen max-w-6xl (1152px, Sektion "Wer dich begleitet") verwendet, korrigiert auf die literale max-w-6xl-Klasse als dokumentierte Ausnahme (keine der drei Container-Breiten trifft 1152px, analog zu CTA.tsx' max-w-2xl). Alle 8 Sektionen anschließend per Diff der gerenderten SSG-HTML (coaching.html) gegen einen main-Build klassenweise verglichen: seitdem bit-identisch bis auf Token-Utility-Namen (text-h2/text-display/max-w-measure statt der alten literalen Werte, alle rechnerisch deckungsgleich, siehe globals.css). Auf main gemergt und live. Journal-Artikel breathe-to-heal-max-strom überarbeitet: neue Headline "Breathe to heal: heilen statt bewältigen", Infoblock zu Max Strom als Hairline-abgesetzter Absatz am Artikelende, mdx-components.tsx um hr-Styling in --color-hairline ergänzt. Live auf main. Assessment v2 auf Branch feature/assessment-v2 umgesetzt nach assessment-v2-spec.md: 7 Fragen, 4 Cluster inkl. mixed, Mehrfachauswahl-Frage F4, zweidimensionale Auswertung Readiness plus Care, modular komponierter Ergebnistext. Noch nicht auf main gemergt, Merge erst nach manuellem Test. Architektur-Refactoring Auftrag 4 von 5 auf Branch refactor/seo-pages umgesetzt: Metadata und JSON-LD vereinheitlicht, restliche Seiten auf die Primitives gestellt. Neuer Helper buildMetadata({ path, title, description, ogType? }) in src/lib/site.ts leitet Top-Level-Felder, openGraph und alternates.canonical (immer über absoluteUrl) aus Titel/Beschreibung ab, genutzt von /, /breathwork, /coaching, /assessment, /termin, /journal, /journal/[slug], /impressum, /datenschutz. metadataBase, Title-Template und Beschreibung stehen jetzt nur noch im Root-Layout (waren byte-identisch im Public-Layout dupliziert), Public-Layout hat keinen eigenen metadata-Export mehr. /assessment, /termin und /journal hatten vorher gar kein eigenes Canonical/OpenGraph und erbten dadurch unbemerkt die Startseiten-Werte aus dem Public-Layout (Canonical zeigte fälschlich auf /); jetzt korrekt pro Seite gesetzt. journal/[slug]/page.tsx nutzt buildMetadata in generateMetadata, die lokal redeklarierte baseUrl-Konstante entfällt, Canonical und mainEntityOfPage laufen über absoluteUrl. Neue Primitive JsonLdScript.tsx (src/components/seo/) für den script-type-application/ld+json-Boilerplate, ersetzt die vier bisherigen Handrollungen (JsonLd.tsx, breathwork/page.tsx, coaching/page.tsx, journal/[slug]/page.tsx), Schema-Inhalte unverändert. Termin/Journal/Assessment/Impressum/Datenschutz sowie die Eyebrow-Nachbauten in AssessmentForm.tsx/AssessmentResult.tsx auf Section/Container/Eyebrow/Heading/Card umgestellt. Card.tsx um eine compact-Padding-Stufe (p-4) für die Termin-Karte erweitert. Impressum/Datenschutz wechseln von festem py-32 auf Section size="default" (reduziertes Mobil-Padding, bewusste Verbesserung), Datenschutz-E-Mail auf hello@lassekluever.de korrigiert (letzte verbliebene Stelle mit der alten gmail-Adresse). Rendered-HTML-Diff gegen main: Seitentitel überall byte-identisch (inklusive der vorbestehenden doppelten Title-Suffixe auf /coaching, /impressum, /datenschutz, bewusst reproduziert, siehe architektur-audit.md), sitemap.xml strukturell unverändert. Auf main gemergt und live. Architektur-Refactoring Auftrag 5 von 5 (letzter Auftrag der Serie) auf Branch refactor/cleanup umgesetzt: ResultActions-Props cluster/result auf die Union-Typen Cluster/ResultRoute aus assessment-config.ts umgestellt (Audit 2.7 Befund B). /api/assessment validiert cluster/route jetzt gegen dieselben Union-Typen (400 bei ungültigem Wert) und prüft answers auf eine plausible Grobstruktur (Objekt, begrenzte Anzahl Einträge, Werte String oder String-Array begrenzter Länge) statt sie ungeprüft zu speichern. JournalMeta (lib/journal.ts) liest jetzt das vollständige dokumentierte Frontmatter (updatedAt, excerpt, coverImage, tags, draft); draft: true wirkt: der Artikel fehlt in getAllJournalEntries() (Übersicht) und getJournalSlugs() (Sitemap, generateStaticParams), die Detailseite liefert in Produktion (NODE_ENV=production) notFound(), bleibt lokal/in Preview-Deployments aber über die direkte URL ansehbar. Kein vorhandener Artikel hatte draft: true gesetzt, keine sichtbare Verhaltensänderung durch diesen Auftrag. Header.tsx ist jetzt eine Server-Komponente, der Logo-Link (usePathname plus smoothScrollToTop) ist als eigene Client-Komponente HeaderLogoLink.tsx ausgelagert (Audit 2.5 Befund B); die beiden Logo-<img>-Tags laufen dabei über next/image (behebt die zwei no-img-element-Lint-Warnings), mit priority (erhält das bisherige eager-Preloading fürs im fixen Header immer sichtbare Logo) und unoptimized (SVG). Alle verbleibenden border-primary/N-Fundstellen (Header-Unterkante, About.tsx' Credentials-Trennstrich, ResultActions.tsx' toter "Bald verfügbar"-Zweig, LeadMagnetForm- und AssessmentForm-Eingabefelder bzw. Antwort-Buttons) auf border-hairline umgestellt (Audit 2.3 Befund B): rechnerisch gegen Surface-Weiß ist --color-hairline (muted bei 30 Prozent, ≈ rgb(211,212,213)) dunkler/kräftiger als das bisherige border-primary/15 (ink bei 15 Prozent, ≈ rgb(220,220,221)), ein eigenes --color-border-input-Token war deshalb nicht nötig; visuelle Freigabe der Eingabefeld-Ränder durch Lasse steht trotzdem noch aus. In globals.css alle @theme-Tokens ohne eine einzige Fundstelle im Code entfernt: --text-h1/-body/-small/-eyebrow, --leading-h1/-body/-small, --opacity-hairline, --spacing-eyebrow-headline/-headline-body, --font-weight-regular/-semibold/-display/-hero, --duration-med (Audit 2.3 Befund A/offene Frage 6). 5 vorbestehende Lint-Fehler und 2 Lint-Warnings behoben (AssessmentForm.tsx' Ref-während-des-Renderns-Schreibzugriffe durch den von React sanktionierten State-Vergleich im Render-Körper plus einen reinen Ref-Reset-Effekt ersetzt, Method.tsx/Transformation.tsx/BreathworkResonance.tsx' unescapte Anführungszeichen auf &quot; umgestellt, Header-img-Warnings s. o.), npm run lint danach ohne Fehler oder Warnings. Doppelte Title-Suffixe auf /coaching, /impressum, /datenschutz behoben (Seitentitel enthielten den Suffix, den das Root-Layout-Title-Template ohnehin anhängt). Rendered-HTML-Diff gegen main: einzige Abweichungen sind die genannten drei Titel und die Rand-Ton-Wechsel auf border-hairline, grep bestätigt keine border-primary/N-Fundstelle mehr im src-Baum. Damit ist die fünfteilige Architektur-Refactoring-Serie aus architektur-audit.md abgeschlossen; offen bleibt nur die optionale, nicht priorisierte Copy-Extraktion aus Audit 2.4 (Home-/breathwork-/coaching-Fließtext- und Array-Konstanten nach src/content bzw. src/lib verschieben). Diese Zeile bei jedem live gegangenen Feature mit aktualisieren. Drei-Stufen-Portfolio (Einzelsession 220 Euro fest, Kompaktbegleitung 6 Sessions à 75 Minuten über etwa 8 Wochen für 1.650 Euro, Hauptprogramm unverändert) auf Branch feature/portfolio umgesetzt: neue Sektion CoachingFormats.tsx (src/components/sections/coaching/) auf /coaching nach CoachingFoundingRound und vor CoachingClosingCTA/CoachingBoundary, kontextfrei nach den Architektur-Konventionen (Section/Container/Eyebrow/Heading/Card, size="compact", border-hairline). Zeigt Einzelsession und Kompaktbegleitung als untergeordnete Karten ohne Buttons, endet mit dem Anrechnungsabsatz ohne eigenen CTA-Link (die direkt folgende Erstgespräch-Sektion hat bereits einen), kein Vergleichs- oder Tabellen-Layout, kein Direktkauf. profil-lasse.md Abschnitt 5 und erstgespraech-leitfaden.md (neuer Abschnitt 11, Zuordnung der drei Stufen) auf die Drei-Stufen-Architektur gebracht. Nach Preview-Abnahme korrigiert: der testweise ergänzte Einzelsession-Absatz in BreathworkClosingCTA.tsx wieder vollständig entfernt (Datei ist wieder bit-identisch mit main), CoachingFormats.tsx gestrafft (size="compact" statt "default", Abschlussabsatz samt Textlink entfernt). Zusätzlich neue Client-Komponente AnchorScrollLink.tsx (src/components/ui/), behebt zwei Fehler des Programm-Anker-Buttons im Coaching-Hero (harter Sprung statt sanftem Scroll, zweiter Klick auf denselben Hash löste kein Scroll mehr aus): scrollt über die bestehende Utility scrollElementToTop (src/lib/scroll.ts, bereits genutzt von AssessmentForm.tsx/AssessmentResult.tsx, keine neue Scroll-Logik nötig), hängt den Hash erst danach per history.replaceState an, Scroll und URL-Zustand sind damit entkoppelt. Button.tsx um ein optionales onClick erweitert, damit AnchorScrollLink.tsx intern das Button-Primitive rendert und dessen Sekundärbutton-Look unverändert übernimmt. Per grep alle gleichartigen Inpage-Anker-Buttons gefunden und umgestellt: CoachingHero.tsx (PROGRAMM_ANCHOR) und BreathworkHero.tsx (AUDIO_RESET_ANCHOR); CoachingClosingCTA.tsx' Cross-Page-Link auf /#audio bewusst unverändert gelassen. Auf main gemergt und gepusht (18.08.2026). Feedback-Seite /feedback (QR-Einladung nach Klassen, Workshops und Einzelsitzungen) auf Branch feature/feedback umgesetzt: 6-schrittiges Formular (format, rating, descriptors, best, improve, contact) nach dem Muster von AssessmentForm.tsx (history-State, Zurück-Button, direktes Weitergehen bei Einzelauswahl), Inhalt in src/lib/feedback-config.ts. ProgressBar.tsx (src/components/ui/) aus AssessmentForm.tsx extrahiert und dort wiederverwendet, keine Verhaltensänderung. Frage-Optik (Nummerierung, choice rows/pills, scale, freetext) folgt der Blocksprache aus design-system.md Abschnitt 8, das dort neu (nur die Feedback-Teilmenge, keine Code-Abhängigkeit auf feature/workbook) entstanden ist, weil main vorher kein Abschnitt 8 hatte. format/src-Query-Parameter steuern Vorbelegung (überspringt Frage 1, über Zurück erreichbar) und Herkunftskennung. Danke-Ansicht (FeedbackThankYou.tsx) zeigt immer erst den Audio-Block, dann die Google-Bewertungseinladung, unabhängig von der Bewertung (kein Review Gating). Neue Tabelle feedback_submissions, Route /api/feedback, Migration supabase/migrations/20260821000000_feedback_submissions.sql (noch nicht gegen Supabase ausgeführt). Absenderkonstanten und escapeHtml aus /api/lead nach src/lib/mail.ts extrahiert, von /api/lead und /api/feedback gemeinsam genutzt. buildMetadata() um noindex-Option erweitert, Button.tsx um external-Prop (target/rel für externe Links). Noch nicht auf main gemergt, kein Merge ohne Freigabe.

## Projekt
Brand- und Akquise-Website für Lasse Klüver. Angebot: Somatic Breathwork und IFS-orientierte Prozessbegleitung. Zielgruppe: zahlungskräftige Menschen mit stressbedingter innerer Unruhe, Anspannung, Erschöpfung. Anmutung: Quiet Luxury, ruhig, klar, autoritativ. Sprache Deutsch. Ziel: Conversion zu kostenfreiem Erstgespräch und zum Audio-Lead-Magneten. Person, Qualifikation, Angebot und Business-Ziele stehen in profil-lasse.md. Konfliktregel: CLAUDE.md für Projekt- und Technikstand, design-system.md für Gestaltung, profil-lasse.md für Person und Angebot.

## Marke
- Entscheidung: Lasse Klüver ist die primäre Identität. "Mato" ist die Methoden- und Markenebene, die später eigenständiger werden kann. Der Bär ist die verbindende Symbolik, der aber nicht bildlich im Logo gezeigt wird.
- Umgesetzt: Lasse Klüver steht vorn in Header, Metadaten, Schema und Footer; Mato dahinter als Methode. Die Seite zeigt "Lasse Klüver", nicht mehr "Mato Coaching".
- Nie "Mato Coaching by Lasse Klüver".
- Erstgespräch: etwa 45 Minuten, Leitfaden in erstgespraech-leitfaden.md (interne Datei, nicht auf die Website, nicht in die Sitemap).

## Domain (Migration abgeschlossen)
- Kanonische Domain: www.lassekluever.de. mato-coaching.de leitet dauerhaft per 301 auf www.lassekluever.de um.
- Alle absoluten URLs im Code zeigen auf https://www.lassekluever.de (über SITE_URL in src/lib/site.ts). Code kanonisiert, Vercel umgeschaltet, Search Console und Google Business aktualisiert. Details in migrations-plan.md.

## Stack
- Next.js 16.2.9, App Router, src/app, Turbopack, TypeScript.
- Tailwind v4, config-less. Tokens in globals.css via @theme sind die Quelle. KEINE tailwind.config.ts.
- Framer Motion sparsam, ui/FadeIn.tsx wiederverwenden.
- Cal.com via @calcom/embed-react unter /termin. Supabase und Resend fürs Backend. MDX fürs Journal.
- Deploy: Vercel via GitHub main. Schriften: Cormorant (font-serif, Display), Hanken Grotesk (font-sans, Body).

## Repo (Kurzüberblick)
- src/app/: layout.tsx (Root, Fonts, metadataBase, Title-Template, Beschreibung — einzige Stelle dafür), icon.svg, sitemap.ts, robots.ts
- src/app/api/: lead/route.ts (Lead-Magnet: Supabase + Resend, Versandstatus), assessment/route.ts (anonyme Abschlüsse), feedback/route.ts (Feedback-Formular: Supabase + Resend-Benachrichtigung, notify_status)
- src/app/(public)/: layout.tsx (Header, Footer, JsonLd, kein eigener metadata-Export mehr), page.tsx (Startseite), assessment/page.tsx, breathwork/page.tsx (Service-Seite, live), coaching/page.tsx (Service-Seite, live), termin/ (page.tsx + CalEmbed.tsx), journal/ (page.tsx + [slug]/page.tsx), feedback/page.tsx (QR-Einladungsseite, noindex, nicht in sitemap.ts)
- src/components/sections/ (Hero, Transformation, Cause, Method, About, CTA, LeadMagnet, LeadMagnetCTA)
- src/components/sections/breathwork/ (BreathworkHero, BreathworkResonance, BreathworkMethod, BreathworkProcess, BreathworkFitFor, BreathworkFAQ, BreathworkAudio, BreathworkClosingCTA; Über-mich-Sektion nutzt About.tsx als BreathworkAbout-Alias, kein eigenes File)
- src/components/sections/coaching/ (CoachingHero, CoachingResonance, CoachingProgram, CoachingMethod, CoachingAbout, CoachingFoundingRound, CoachingFormats, CoachingClosingCTA, CoachingBoundary; CoachingAbout ist eine eigene Datei, kein About.tsx-Alias, weil der Text speziell auf das Programm zugeschnitten ist und von About.tsx abweicht)
- src/components/forms/ (AssessmentForm, LeadMagnetForm, ResultActions, FeedbackForm, FeedbackThankYou)
- src/components/ui/ (Header als Server-Komponente, HeaderLogoLink als deren einziger Client-Teil, Footer, FadeIn, Eyebrow, ListMark, StepList, ProgressBar, Section, Container, Heading, Button, Card, AnchorScrollLink als Client-Teil für sanft scrollende Inpage-Anker-Buttons)
- src/components/seo/ (JsonLd.tsx: globales ProfessionalService-Schema, JsonLdScript.tsx: gemeinsame script-type-application/ld+json-Primitive für alle vier JSON-LD-Konsumenten)
- src/content/journal/<slug>.mdx; src/lib/ (assessment-config.ts, feedback-config.ts, journal.ts, supabase.ts, mail.ts, scroll.ts, anchors.ts, site.ts mit buildMetadata())
- supabase/migrations/ (SQL-Migrationen, bisher nur die Feedback-Tabelle; leads/assessment_submissions wurden ursprünglich ohne Migrationsdatei angelegt)

## Design (Details in design-system.md)
- Farben: background #fcfaf0, surface #ffffff, primary #19191a, accent #09173b (CTAs), muted #6b6e72, umber #7c6a57 (warmer Akzent, sparsam, Eyebrows und Hairlines).
- Display Cormorant, Body Hanken Grotesk. Section-Padding py-16 md:py-24. Buttons rounded-md, keine Schatten. Eyebrow = Umber-Hairline plus Uppercase-Label.

## Architektur-Konventionen
- Sektionen (src/components/sections/**) sind kontextfrei: jede Sektion bringt ihren eigenen Section-Wrapper und ihre eigenen FadeIns vollständig selbst mit. Seiten (src/app/(public)/**/page.tsx) sind reine Kompositionslisten aus Sektionen, ohne äußere FadeIn-Wrapper und ohne sonstige Layout-Logik. Grund: dieselbe Sektion kann auf mehreren Seiten eingesetzt werden (z. B. About.tsx auf / und als BreathworkAbout-Alias auf /breathwork) und muss sich dort identisch verhalten.
- Wiederkehrende UI-Muster laufen ausschließlich über die Primitives in src/components/ui/: Section (Padding), Container (Breite), Heading (Überschriftengrößen), Button (CTA-Links), Card (Kartenrand/-radius), Eyebrow (Umber-Label), ListMark (Umber-Hairline-Strich vor Listen-/Schrittpunkten). Keine handgerollten Klassenketten für diese Muster in Sections, Forms oder Pages.
- Hairline-Marker vor Listen- und Schrittpunkten ausschließlich über ListMark (h-[1lh] plus items-center statt geratener margin-top-Werte). Das gemeinsame Elternelement (i. d. R. das li) muss dieselbe Schriftgrößen-/Zeilenhöhen-Klasse tragen wie der Text daneben, sonst bezieht sich 1lh auf die falsche Zeile. Ausnahme: Eyebrow.tsx hat einen eigenen, bereits zentrierten Strich und bleibt unabhängig davon.
- Section-Padding ausschließlich über die size-Prop (compact/default/spacious), nie als eigene py-*-Klasse am Aufrufer. Containerbreiten ausschließlich über Container (width prose/narrow/default), nie als eigene max-w-*-Klasse am Aufrufer.
- Ausnahmen sind erlaubt, wenn eine Sektion nachweislich von der gemeinsamen Größe abweicht (z. B. CTA.tsx', BreathworkClosingCTA.tsx' und CoachingClosingCTA.tsx'/CoachingBoundary.tsx' schmalere Textspalte max-w-2xl) — dann bleibt die betroffene Stelle bewusst außerhalb des Primitives, statt sie unbeabsichtigt zu verkleinern/verbreitern oder ein Primitive mit widersprüchlicher className zu überladen (Cascade-Reihenfolge bei zwei Utilities auf derselben CSS-Property ist nicht garantiert quellcode-Reihenfolge).
- Textspalten-Regel: Container zentriert immer per mx-auto als äußerer Rahmen einer Sektion. Textspalten, die enger als ihr umgebender Container sind, aber (anders als Container width="prose") nicht selbst zentriert sein dürfen, bekommen ihre Breite als max-w-measure-Klasse (Token, kein Arbitrary-Value max-w-[68ch]) direkt am Element, statt sie in ein zusätzliches, zentrierendes Container-Primitive zu verschachteln. Gilt für die Fließtext-Blöcke in breathwork/*.tsx und coaching/*.tsx gleichermaßen.
- Anchor-Regel: Inpage-Sprungziele (Section-/section-id sowie interne href="#..."- bzw. href="/#..."-Links) laufen ausschließlich über benannte Konstanten in src/lib/anchors.ts, nie als wörtlicher String an ID-Definition und Link-Stelle getrennt gepflegt. Inpage-Anker-Buttons (Ziel liegt auf derselben Seite) laufen über AnchorScrollLink.tsx (src/components/ui/), nicht über einen nackten Hash-href: sanftes Scrollen über scrollElementToTop (src/lib/scroll.ts), Hash erst danach per history.replaceState gesetzt, damit auch ein zweiter Klick auf denselben Anker funktioniert. Cross-Page-Anker-Links (z. B. /#audio von einer anderen Seite aus) bleiben normale Links, die laufen über echte Navigation.
- Heading kennt neben section eine emphasis-Variante für Sektionen, die bewusst stärker betont werden sollen: auf Mobil identisch mit section, ab md eine Stufe größer (48px, --text-h2-emphasis-md). Aktuell einzige Verwendung: Cause.tsx auf der Startseite. Zusätzlich eine display-sub-Variante für Service-Hero-H1 (BreathworkHero.tsx, CoachingHero.tsx): dieselben Tokens wie display, aber ohne lg-Stufe (wächst nicht bis 72px).
- Neue Seiten folgen dem Muster sections/<seite>/*.tsx (eigener Unterordner pro Seite, wie bereits bei breathwork/ und coaching/ etabliert), keine Inline-JSX-Sektionen in der page.tsx.
- Type-Scale lebt in globals.css (--text-display/-display-md/-display-lg, --text-h2/-h2-md/-h2-emphasis-md, --leading-display/-h2) plus Heading.tsx; Containerbreiten in globals.css (--container-content, --container-measure) plus Container.tsx. Schriftgrößen für Headlines sind nur noch an diesen beiden Stellen definiert.
- Seiten-Metadata ausschließlich über buildMetadata({ path, title, description, ogType? }) aus src/lib/site.ts, nie eigene title/description/openGraph/alternates-Objekte pro Seite von Hand zusammensetzen. Ausnahme: die Startseite ergänzt danach ihr eigenes, für Social-Previews formuliertes openGraph (Bild, Locale, Siegename), das bewusst vom Seitentitel abweicht. metadataBase, Title-Template und die Fallback-Beschreibung stehen ausschließlich im Root-Layout (src/app/layout.tsx).
- JSON-LD ausschließlich über die Primitive JsonLdScript.tsx (src/components/seo/) für den script-type-application/ld+json-Rahmen, die Schema-Objekte selbst bleiben beim jeweiligen Aufrufer. Zwei JSON-LD-Blöcke pro Journal-Artikel sind bewusst so: das globale ProfessionalService-Schema (JsonLd.tsx im Public-Layout) läuft auf jeder Seite, Artikel haben zusätzlich ihr eigenes Article-Schema. Das ist keine Redundanz, sondern zwei unterschiedliche Entitäten (Unternehmen vs. Artikel).
- Ränder für Trennlinien, Kartenränder und Formular-Eingabefelder/Antwort-Buttons laufen ausschließlich über border-hairline (--color-hairline), nie über border-primary/N oder eine andere Opazitätsstufe. Kein eigenes Input-Token: --color-hairline ist gegen Surface-Weiß rechnerisch dunkler/kräftiger als das frühere border-primary/15, deckt die Eingabefeld-Rolle also mit ab (siehe architektur-audit.md, Status-Block).
- Server/Client-Grenzen so klein wie möglich schneiden: enthält eine Komponente nur einen kleinen interaktiven Teil (Klick-Handler, Hook), diesen Teil als eigene "use client"-Komponente auslagern (Muster: Header.tsx/HeaderLogoLink.tsx) statt die ganze Komponente client-seitig zu machen.
- Wiederverwendung vor Neubau: Vor jeder neuen Utility, Komponente oder Logik zuerst prüfen, ob src/lib/ oder src/components/ui/ bereits eine Lösung für dasselbe Problem enthält (lesen und grep, nicht raten). Bestehendes wird genutzt oder erweitert, nie parallel neu gebaut. Gleiche Verhaltensmuster (Scrollen, Fokus, Motion, Formatierung) haben genau eine Implementierung im Repo. Wer eine Lücke füllt, erweitert die bestehende Datei am bestehenden Ort, statt eine neue Datei mit ähnlichem Zweck anzulegen.

## Assessment (/assessment)
- Assessment v2, umgesetzt nach assessment-v2-spec.md (Statuszeile dort trägt das Umsetzungsdatum). Inhalt und Logik getrennt in src/lib/assessment-config.ts. Cluster = exhaustion|tension|panic|mixed, ResultRoute = ready|almost|not_yet.
- 7 Fragen: q1 setzt den Cluster (inkl. mixed), q2/q4/q5/q6/q7 gemeinsam für alle Cluster, q3 hat vier Varianten (q3_exhaustion/q3_tension/q3_panic/q3_mixed) und verzweigt nach Cluster. q4 ist eine Mehrfachauswahl-Frage (Körperempfindungen) mit einer exklusiven "weiß nicht"-Antwort, die alle anderen abwählt und umgekehrt abgewählt wird, sobald etwas anderes gewählt wird. Kein Überspringen mehr, jede Frage muss beantwortet werden.
- Scoring: readiness_signal zählt 1 Punkt, readiness_soft 0,5 Punkte. Schwellen: Score >=4 ready, >=2,5 und <4 almost, sonst not_yet. Care-Logik zusätzlich und unabhängig von der Route: careActive, wenn mindestens 1 care_signal-Tag oder mindestens 2 care_signal_soft-Tags gesammelt wurden. calculateResult(collectedTags) gibt { route, careActive } zurück.
- Ergebnistext ist modular aus den tatsächlich gewählten Antworten komponiert (composeResult in assessment-config.ts), nicht mehr statisch pro Route/Cluster. Bausteine in fixer Reihenfolge: Headline und Einstieg nach Cluster, Spiegelbaustein zur F3-Antwort, Körperabsatz zu den F4-Auswahlen (inkl. Sonderfällen Taubheit und "nie geachtet"), Spiegelbaustein zur F5-Antwort, Nervensystem-Absatz nach Cluster, optionale Zusatzsätze (insight_no_relief, chronic_pattern), optionaler Care-Absatz, optionaler Modifikator (need_embodiment oder need_companionship) und der Routentext mit optionalem search_fatigue-Einschub bei ready/almost. Ein Baustein erscheint nur, wenn sein Tag durch eine tatsächlich gewählte Antwort gesammelt wurde.
- AssessmentForm: history-State (inkl. Antwort-IDs pro Frage), Zurück-Button bleibt, Umber-Fortschritt. Einzelauswahl-Fragen wählen direkt und gehen weiter, q4 togglet Auswahlen und hat einen eigenen Weiter-Button (aktiv ab mindestens einer Auswahl). POSTet den Abschluss einmal an /api/assessment (answers als Objekt questionId → Antwort-ID, bei q4 als Array), gibt cluster und route an AssessmentResult/ResultActions.
- AssessmentResult (neu, src/components/forms/AssessmentResult.tsx): rendert Headline (Cormorant, H1/Section-Größe) und die komponierten Absätze als ruhigen Lesetext (max-w-[68ch], kein Kartenrahmen), danach den leisen Link "Assessment neu starten" (setzt den Form-State zurück), danach ResultActions.
- ResultActions: unverändert für alle drei Routen (keine Sonder-Variante für not_yet, die Lenkung übernimmt der Routentext). Drei Karten (Erstgespräch, Audio, Journal), Erstgespräch-Karte verlinkt immer /termin?cluster=...&result=..., cluster kann jetzt auch "mixed" sein. Buttons gleich breit (max-w-62, 15.5rem), zentriert. Lead-in-Satz über dem Audio-Formular blendet nach erfolgreichem Absenden aus (onSuccess).

## Journal (/journal)
- Gebaut. Artikel als src/content/journal/<slug>.mdx. Frontmatter: title, description, publishedAt (ISO), optional updatedAt, excerpt, coverImage, tags, draft. Datumsfeld heißt publishedAt. JournalMeta (lib/journal.ts) liest alle diese Felder ein (tags Default [], draft Default false).
- draft: true wirkt: getAllJournalEntries() (Übersicht) und getJournalSlugs() (Sitemap, generateStaticParams) lassen den Artikel weg. getJournalEntryBySlug() liefert für einen draft-Artikel in Produktion (NODE_ENV=production) null, die Detailseite ruft dann notFound(); lokal und in Preview-Deployments bleibt der Artikel über die direkte URL ansehbar.
- Übersicht plus Detailseite (MDX, Article-JSON-LD), Sitemap nimmt Artikel auf. JOURNAL_READY in ResultActions ist true.
- Vorhanden: was-ist-somatic-breathwork, was-bei-einer-panikattacke-passiert, breathe-to-heal-max-strom. Regel: erklären, nicht behandeln.

## Feedback (/feedback)
- Reine QR-Einladungsseite (in Klassen, Workshops, Einzelsitzungen gezeigt), keine SEO-Seite: buildMetadata({ noindex: true }), nicht in sitemap.ts, kein Header-Nav-Link. Query-Parameter format (klasse|workshop|einzel, belegt Frage 1 vor und überspringt sie, über Zurück erreichbar) und src (freier Kurzname, nur [a-z0-9-]{1,40}, als source gespeichert).
- Inhalt in src/lib/feedback-config.ts, Muster wie assessment-config.ts. 6 Schritte in fester Reihenfolge, Frage-IDs sind stabil und werden nicht umbenannt: format (choice single, rows), rating (scale 1–5, bewusst 5 statt der 7 Stufen aus dem Workbook, damit der Wert gegen Google-Bewertungen lesbar bleibt), descriptors (choice multi, pills, 1–3 aus 12 Optionen), best (freetext, Pflicht, 3–1000 Zeichen), improve (freetext, optional, überspringbar, max. 1000 Zeichen), contact (eigener Schritt: Name/E-Mail optional, zwei unabhängige Checkboxen contactConsent/quoteConsent, E-Mail erfordert contactConsent).
- FeedbackForm.tsx: history-State und Zurück-Button nach dem Muster von AssessmentForm.tsx, ProgressBar.tsx (Umber, aus AssessmentForm.tsx extrahiert) für den Fortschritt. Frage-Optik (Nummerierung "01"–"06", choice rows/pills, scale, freetext) folgt design-system.md Abschnitt 8, eigenständig für /feedback gebaut, keine Code-Abhängigkeit auf feature/workbook.
- POSTet einmal an /api/feedback. FeedbackThankYou.tsx (Danke-Ansicht): erst der Audio-Block (Card, NEXT_PUBLIC_FEEDBACK_AUDIO_URL, kein stiller Fallback bei fehlender Env — Warnung im Server-Log, Block entfällt), dann "Wie es weitergehen kann" (Umber-Hairline, Muster aus LeadMagnetForm-Erfolgsansicht), dann die Google-Bewertungseinladung (NEXT_PUBLIC_GOOGLE_REVIEW_URL, ohne Env nur der Mailto-Button). Grundsatz: Google-Einladung für alle, keine Verzweigung nach Bewertung (Googles Richtlinie gegen Review Gating, UWG).
- Refactoring-Kandidaten (zurückgestellt, keine größeren Eingriffe in AssessmentForm.tsx/LeadMagnetForm.tsx für diesen Auftrag): AssessmentForm.tsx' Einzel-/Mehrfachauswahl-Buttons und FeedbackForm.tsx' choice-rows/-pills sind zwei getrennte Implementierungen; LeadMagnetForm.tsx' Input-/Checkbox-Styling wurde in den Kontakt-Schritt kopiert statt in ein gemeinsames Feld-Primitive extrahiert. Beides Kandidaten für eine gemeinsame Konsolidierung, idealerweise zusammen mit dem Workbook-Merge.

## Backend
- Lead-Magnet: LeadMagnetForm POSTet an /api/lead. Die Route speichert in Supabase, sendet Audio-Mail an die Person und Benachrichtigung an Lasse (Resend-Fehler prüfen). Absender "Lasse Klüver · Mato Coaching <hello@lassekluever.de>", Reply-To hello@lassekluever.de.
- LeadMagnetForm props: autoFocus, source, assessmentCluster, assessmentResult, onSuccess (feuert einmalig bei status "success", nutzen LeadMagnetCTA und ResultActions zum Ausblenden ihrer Einladungssätze). Sendet pagePath und referrer mit, scrollt bei Erfolg sanft nach oben.
- /api/assessment speichert anonyme Abschlüsse, versendet keine Mail.
- /api/feedback: speichert zuerst (Fehler beim Mailversand rollen das nicht zurück), sendet danach eine Benachrichtigung an LEAD_NOTIFICATION_EMAIL ("Bitte persönlich antworten" oben in der Mail bei rating <= 3 oder ausgefülltem improve), schreibt das Ergebnis in notify_status (pending/sent/failed).
- Gemeinsame Resend-Helfer (Absenderadresse, Reply-To, escapeHtml, isValidEmail) in src/lib/mail.ts, genutzt von /api/lead und /api/feedback, damit beide Routen dieselbe Absenderkonstante haben.
- Resend: Domain lassekluever.de verifiziert (DKIM, SPF, send-MX über die Strato-Subdomain send.lassekluever.de). Root-MX = Strato Mailserver (Postfach hello@). RESEND_API_KEY ist Full-Access, nicht domain-beschränkt.
- Supabase-Keep-alive-Cron: src/app/api/cron/keep-alive/route.ts, per Vercel Cron (vercel.json) montags und donnerstags 6:00 UTC aufgerufen. Zweck: Das Supabase-Free-Projekt pausiert nach 7 Tagen Inaktivität, das hat POST /api/lead in Produktion bereits einmal fehlschlagen lassen. Die Route macht einen minimalen Read (count auf leads, keine Daten geloggt) und prüft den Bearer-Header gegen CRON_SECRET. Entfällt nach dem Upgrade auf Supabase Pro (siehe offene Aufgaben).

## Datenbank (RLS an, keine Public-Policy, nur Server schreibt)
- leads: id, created_at, email, name, consent, source, page_path, referrer, assessment_cluster, assessment_result, audio_email_status (pending dann sent oder failed). Spalte heißt audio_email_status.
- assessment_submissions: id, created_at, cluster, result_route, answers (jsonb).
- feedback_submissions: id, created_at, format (klasse|workshop|einzel), rating (1–5), descriptors (text[]), best, improve (nullable), name, email (beide nullable), contact_consent, quote_consent, source, page_path, notify_status (pending dann sent oder failed). Migration: supabase/migrations/20260821000000_feedback_submissions.sql, noch nicht gegen Supabase ausgeführt (in Vercel/Supabase manuell nachziehen).
- Keine IP speichern. User-Agent nur in der Lead-Magnet-Mail, nicht in der DB (Feedback speichert gar kein User-Agent, auch nicht in der Mail).

## Env (in Vercel, Werte nie im Code)
NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, RESEND_API_KEY, LEAD_NOTIFICATION_EMAIL (hello@lassekluever.de), LEAD_AUDIO_URL (https://www.lassekluever.de/audio/physiological-sigh.m4a), CRON_SECRET (autorisiert den Vercel-Cron-Aufruf gegen /api/cron/keep-alive, von Vercel selbst als Bearer-Header mitgeschickt), NEXT_PUBLIC_GOOGLE_REVIEW_URL (direkter "Bewertung schreiben"-Link aus dem Google-Unternehmensprofil, für die Feedback-Danke-Ansicht), NEXT_PUBLIC_FEEDBACK_AUDIO_URL (absolute URL zum Dankeschön-Audio der Feedback-Seite). Struktur und Platzhalter in .env.example.

## SEO
- sitemap.ts und robots.ts in src/app. JsonLd: ProfessionalService, areaServed Hamburg, founder Lasse Klüver (sameAs LinkedIn). Unternehmens-sameAs leer (später Instagram).
- knowsAbout enthält bewusst "Anxiety" und "Panikattacken" als Wissensgebiete (Inhaberentscheidung, nicht entfernen). Die Begriffe dürfen jetzt auch im sichtbaren Text nach der Ampel-Logik der Copy-Regeln genutzt werden.
- Bei neuen Seiten die Sitemap erweitern und mit buildMetadata() (siehe Architektur-Konventionen) mindestens Titel, Beschreibung und Canonical setzen. Per-Page-Metadaten nur in Server-Komponenten.
- Jede Seite hat genau ein Canonical (absolute www.lassekluever.de-URL über absoluteUrl()) und eigenes openGraph. /breathwork und /coaching haben zusätzlich ihr Service-Schema, Journal-Artikel zusätzlich ihr Article-Schema, jeweils über JsonLdScript.tsx.

## Copy- und Rechtsregeln
- Anxiety und Panikattacken im sichtbaren Text nach dieser Ampel-Logik (v2):

  Grün: Erlebens-Nennungen überall ('für Menschen mit Panikattacken', 'wenn du Anxiety kennst'). Edukative Journal-Artikel vollständig frei, inklusive hoffnungsvoller Aussagen, solange sie nicht als Versprechen des Programms formuliert sind. Teilnehmerstimmen als gekennzeichnete Zitate. Themenbenennende Metadaten. 'Hilfe bei Anxiety' und 'Begleitung bei Anxiety' sind erlaubt.
  Rot, nie: heilt, löst auf, beseitigt, befreit von, überwindet plus klinischer Begriff. Ergebniszusagen mit klinischen Begriffen. 'Behandlung von' oder 'Therapie für' plus klinischer Begriff. 'Hilfe bei Panikattacken' als Leistungsversprechen, stattdessen das für-Menschen-mit-Muster.
  Edukations-Leitplanke: In Journal-Artikeln keine direkte Verknüpfung von klinischem Begriff, Wirkzusage und dem Angebot im selben Absatz.
  Weiterhin keine Diagnosen oder Behandlungszusagen. Framing des Angebots bleibt Nervensystem-Regulation, Stressregulation, Persönlichkeitsentwicklung.
- Footer-Disclaimer sinngemäß: "...ersetzt keine psychotherapeutische oder ärztliche Behandlung."
- Stil: Sentence case, aktive Verben, keine Floskeln, KEINE Gedankenstriche.
- Nie "tägliche Audio-Praxis", "tägliche Audios" oder Formulierungen, die eine Audio-Datei pro Tag suggerieren. Immer "Übungen und Audios für die tägliche Praxis" oder "Audio-Material für die tägliche Praxis".
- DSGVO: Einwilligung im Formular. Die Datenschutzerklärung muss source, page_path, referrer, assessment_cluster, assessment_result und audio_email_status abdecken, sowie (Feedback-Formular) Format, Bewertung, Beschreibungsworte, Freitexte, optional Name/E-Mail, contact_consent/quote_consent und die QR-Herkunftskennung. Vor Newsletter Double-Opt-in. Auftragsverarbeitung mit Supabase und Resend.

## Offene Aufgaben
1. ~~Domain-Migration~~ (erledigt). Alle Phasen abgeschlossen, Details in migrations-plan.md.
2. Restliche Service-Seite /ifs. Am /breathwork-Muster orientieren: Server-Komponente, eigene Metadaten über buildMetadata() (src/lib/site.ts), Service-JSON-LD inline auf der Seite über JsonLdScript.tsx (nicht über das globale JsonLd.tsx, das bleibt für ProfessionalService reserviert), Sitemap erweitern.
   Echte Header-Navigation erst entscheiden, wenn alle drei Service-Seiten stehen. Bewusst KEIN Header-Nav-Link für einzelne Seiten; Header bleibt auf den /termin-CTA reduziert.
3. Hör-Tracking des Audios (eigene Hörseite plus Token pro Lead, DSGVO-sensibel, eigener Auftrag mit Datenschutz-Absatz).
4. Weitere Journal-Artikel. AggregateRating sobald Bewertungen. OG-Bild 1200×630. Instagram in sameAs. Assessment-Videos.
5. Robuster Env-Umgang für LEAD_AUDIO_URL: In src/app/api/lead/route.ts keinen stillen Fallback auf eine hart verdrahtete Audio-URL verwenden. Fehlt die Env-Variable, eine Warnung ins Server-Log schreiben, statt lautlos eine Datei-URL zu raten. Grund: Ein stiller Fallback hat beim Domainwechsel einen falschen Link verdeckt.
6. Digitales IFS-Workbook für Klienten. Konzept, Architektur und Auftragsfahrplan stehen in workbook-konzept.md. Entwicklung ausschließlich auf dem Branch feature/workbook, Merge auf main erst nach MVP-Abschluss.
7. Supabase Pro Upgrade spätestens zum Start der Gründungsrunde, danach Keep-alive-Cron entfernen.
8. Optionale, nicht priorisierte Copy-Extraktion (Audit 2.4, Kandidat außerhalb der fünfteiligen Architektur-Refactoring-Serie): Home-/breathwork-/coaching-Fließtext- und Array-Konstanten (FAQ, Prozess-Schritte, Credentials, Benefits) nach src/content bzw. src/lib verschieben, analog zu assessment-config.ts. Kein Termin, nur bei Bedarf.
9. Feedback-Audio produzieren und hochladen (public/audio/...), NEXT_PUBLIC_FEEDBACK_AUDIO_URL und NEXT_PUBLIC_GOOGLE_REVIEW_URL in Vercel setzen, Migration supabase/migrations/20260821000000_feedback_submissions.sql gegen Supabase ausführen, QR-Codes pro Klasse/Kurstag erzeugen (Ziel-URL /feedback?format=...&src=...), danach feature/feedback nach manuellem Test auf main mergen.
10. Teilnehmerstimmen-Sektion aus quote_consent-Einträgen in feedback_submissions (später, erst ab etwa zehn Einträgen; Auswahl und Freigabe manuell durch Lasse, keine automatische Veröffentlichung).

## Erledigt
- Inhaltliche Marken-Umstellung: Lasse vorn, Mato als Methode, in Header, Metadaten, Schema, Footer.
- Service-Seite /breathwork: umgesetzt und live auf www.lassekluever.de.
- /breathwork intern verlinkt: kontextueller Textlink in der Method-Section ("Wie Somatic Breathwork wirkt") und dezenter Footer-Link ("Somatic Breathwork"). Bewusst kein Header-Link.
- Service-Seite /coaching: live, mit Gründungsrunden-Konditionen (4 Plätze, 2.900 Euro, regulär ab 3.900 Euro) im sichtbaren Text. Copy-Prinzip der Seite: Outcomes vorn, Methoden als Begründung in eigener Sektion. Nach Abschluss der Gründungsrunde muss die Seite aktualisiert werden.
- PNG-Favicons ergänzt: src/app/icon.png (512×512) und src/app/apple-icon.png (180×180) neben icon.svg, damit Google in den Suchergebnissen zuverlässig das Icon zeigt. Next.js erzeugt automatisch <link rel="icon"> und <link rel="apple-touch-icon">.
- Lighthouse-Fixes (07.07.2026): Footer-Kontrast auf WCAG AA angehoben (/40 und /50 → /60 auf dunkelgrundigen Texten), Header-Logo-imgs mit width/height-Attributen dimensioniert (unsized-images), Hero-Einblendung von Framer Motion auf CSS-Animation umgestellt für früheres LCP (Keyframe hero-fade-up in globals.css, gestaffelte .hero-d1/.hero-d2/.hero-d3-Delays, FadeIn-Wrapper im Hero entfernt).
- Architektur-Refactoring-Serie nach architektur-audit.md (5 Aufträge, alle auf main gemergt und live): UI-Primitives Section/Container/Heading/Button/Card/Eyebrow/ListMark/StepList (Auftrag 1), /breathwork und /coaching vollständig auf die Primitives migriert (Auftrag 2/3), Metadata/JSON-LD über buildMetadata()/JsonLdScript.tsx vereinheitlicht (Auftrag 4), Typisierung (ResultActions, /api/assessment), Journal-draft-Filterung, Header/HeaderLogoLink-Client-Grenze, border-hairline statt border-primary/N, ungenutzte @theme-Tokens entfernt, Lint komplett sauber, doppelte Title-Suffixe behoben (Auftrag 5). Details je Auftrag in den Stand-Einträgen oben, abschließender Status je Befund in architektur-audit.md.

## Perspektivisch
Datenschutzfreundliche Analytics (Plausible, cookieless). Double-Opt-in vorbereiten. Lead-Dedup nach E-Mail. Resend-Webhook für "delivered". Cal.com-Webhook für Buchung in Supabase. Lokale Testumgebung (.env.local plus .env.local.example): separates Supabase-Dev-Projekt oder diszipliniert gegen Produktion (echte Tests schreiben in die DB und versenden echte Mails).

## Off-Code (keine Claude-Code-Aufgaben)
Google Business auf "Lasse Klüver" umbenannt, verifiziert. Search Console verifiziert, Sitemap eingereicht, Adressänderung ausgeführt. Bewertungen einsammeln ist der lokale Hebel. Keine bezahlten Anzeigen, bis der Funnel konvertiert.
Google-Business-Leistungen und Coachday-Profil an die neue Drei-Stufen-Architektur angleichen: Einzelsession auf festen Preis 220 Euro umstellen (Spanne entfernen), Kompaktbegleitung (6 Sessions à 75 Minuten über etwa 8 Wochen, 1.650 Euro) ergänzen.

## Arbeitsweise
- Kleine Aufgaben, ein Feature pro Sitzung. Tokens, Schriften, Spacing aus globals.css und design-system.md. FadeIn wiederverwenden. Häufig committen, vor Größerem ein Branch.
- Workbook-Aufträge committen und pushen immer auf origin feature/workbook, nie auf main.
- Dev-Hinweis: @theme in globals.css wird ggf. als unbekannte At-Rule markiert (harmlos). "Tailwind CSS IntelliSense" installieren oder css.lint.unknownAtRules auf ignore.
